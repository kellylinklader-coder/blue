# Blue Eye - Google Colab Setup Guide\n\n## Quick Setup (Copy & Paste)\n\nOpen Google Colab and run these cells in order:\n\n### **Cell 1: Clone Repository**\n\n```python\n!git clone https://github.com/kellylinklader-coder/blue.git\n%cd blue\n```\n\n### **Cell 2: Install Dependencies**\n\n```python\n!pip install -r requirements.txt\n```\n\n### **Cell 3: Run Blue Eye Interactive**\n\n```python\n!python blue_eye.py\n```\n\nThen follow the prompts:\n- Enter target domain (e.g., `example.com`)\n- Enter company name (e.g., `example`)\n\n### **Cell 4: Run Blue Eye with Specific Target**\n\n```python\n!python blue_eye.py -t example.com -n \"Example Inc\"\n```\n\n### **Cell 5: Download Report**\n\n```python\nfrom google.colab import files\nimport os\n\n# List available reports\nprint(\"\\nAvailable reports:\")\nos.system('ls -lh reports/')\n\n# Download the latest report\nos.system('ls -t reports/ | head -1 | xargs -I {} cp reports/{} latest_report.html')\nfiles.download('latest_report.html')\n```\n\n---\n\n## Full Colab Notebook Template\n\nCreate a new cell and paste this complete workflow:\n\n```python\n# ============================================\n# BLUE EYE RECON - GOOGLE COLAB SETUP\n# ============================================\n\nprint(\"[+] Step 1: Cloning Blue Eye Repository...\")\n!git clone https://github.com/kellylinklader-coder/blue.git\n%cd blue\n\nprint(\"\\n[+] Step 2: Installing Dependencies...\")\n!pip install -r requirements.txt\n\nprint(\"\\n[+] Step 3: Running Blue Eye Scan...\")\nprint(\"\\nEnter the domain and company name when prompted:\\n\")\n\n# Run with specific target (edit these values)\nTARGET = \"example.com\"  # Change this\nCOMPANY = \"Example Inc\"  # Change this\n\n!python blue_eye.py -t {TARGET} -n \"{COMPANY}\"\n\nprint(\"\\n[+] Step 4: Preparing report for download...\")\nimport os\nfrom google.colab import files\n\nif os.path.exists('reports'):\n    os.system('ls -lh reports/')\n    # Download latest report\n    latest = os.popen('ls -t reports/ | head -1').read().strip()\n    os.system(f'cp reports/{latest} blue_eye_report.html')\n    print(f\"\\n[+] Downloading: {latest}\")\n    files.download('blue_eye_report.html')\nelse:\n    print(\"[!] No reports directory found\")\n```\n\n---\n\n## Step-by-Step Instructions\n\n### **1. Open Google Colab**\n- Go to: https://colab.research.google.com/\n- Click \"+ New notebook\"\n\n### **2. Copy This Into First Cell**\n\n```python\n!git clone https://github.com/kellylinklader-coder/blue.git\n%cd blue\n!pip install -r requirements.txt\n```\n\n- Click the play button (▶️) to run\n- Wait for installation to complete\n\n### **3. Copy This Into Second Cell**\n\n```python\n!python blue_eye.py -t google.com -n \"Google\"\n```\n\n- Replace `google.com` with your target\n- Replace `Google` with company name\n- Click play (▶️)\n\n### **4. Copy This Into Third Cell to Download Report**\n\n```python\nfrom google.colab import files\nimport os\n\n# Find and download the report\nos.system('ls -t reports/ | head -1 | xargs -I {} cp reports/{} report.html')\nfiles.download('report.html')\n```\n\n- Click play (▶️)\n- File will download automatically\n\n---\n\n## Advanced Options in Colab\n\n### **Disable Certain Modules**\n\n```python\n!python blue_eye.py -t example.com -n \"Example\" --disable-modules github_users email_harvesting\n```\n\n### **Enable Only Specific Modules**\n\n```python\n!python blue_eye.py -t example.com --enable-modules subdomains technologies dns_records\n```\n\n### **Custom Config File**\n\n```python\n# Upload custom config\nfrom google.colab import files\nfiles.upload()  # Upload config.yaml\n\n# Run with custom config\n!python blue_eye.py -c config.yaml -t example.com\n```\n\n### **View All Reports Generated**\n\n```python\nimport os\nprint(\"All reports:\")\nos.system('ls -lh reports/')\n```\n\n### **Download All Reports**\n\n```python\nimport shutil\nfrom google.colab import files\n\n# Create zip of all reports\nshutil.make_archive('blue_eye_reports', 'zip', 'reports')\nfiles.download('blue_eye_reports.zip')\n```\n\n---\n\n## Troubleshooting\n\n### **Error: \"pip not found\"**\n```python\n!pip3 install -r requirements.txt\n```\n\n### **Error: \"ModuleNotFoundError\"**\n```python\n# Reinstall specific module\n!pip install dnspython requests python-nmap PyYAML\n```\n\n### **Error: \"reports directory not found\"**\n```python\n# Create reports directory\nimport os\nos.makedirs('reports', exist_ok=True)\n```\n\n### **Slow DNS lookups in Colab**\n- This is normal, DNS queries can be slow\n- Try disabling DNS-heavy modules:\n```python\n!python blue_eye.py -t example.com --disable-modules dns_records subdomains\n```\n\n---\n\n## Complete Minimal Example\n\nCopy and paste this as one cell:\n\n```python\n# Install\n!git clone https://github.com/kellylinklader-coder/blue.git\n%cd blue\n!pip install -r requirements.txt\n\n# Scan\n!python blue_eye.py -t github.com -n \"GitHub\"\n\n# Download\nfrom google.colab import files\nimport os\nos.system('cp reports/blue_eye_report_github_com* report.html 2>/dev/null || ls reports/')\nif os.path.exists('report.html'):\n    files.download('report.html')\n```\n\n---\n\n## Tips\n\n✅ **Faster scans** - Disable heavy modules:\n```python\n!python blue_eye.py -t example.com --disable-modules nmap_scan email_harvesting\n```\n\n✅ **Keep Colab running** - Enable notebooks in background\n\n✅ **Free resources** - Colab gives you free compute, but has usage limits\n\n✅ **Persistent storage** - Files in `/content` are temporary (cleared after 12 hours)\n\n---\n\n## Save Your Notebook\n\n1. Click \"File\" → \"Save\"\n2. Choose \"Save to GitHub\" to backup your work\n3. Or download as `.ipynb` file\n\n---\n\n**Happy Hacking!** 🎯\n"